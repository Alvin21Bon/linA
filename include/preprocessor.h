#ifndef LINA_PREPROCESSOR_H
#define LINA_PREPROCESSOR_H

#include <math.h>

static inline double TO_RADIANS(double degrees)
{
	return (degrees * (M_PI / (double)180));
}

#endif // LINA_PREPROCESSOR_H
